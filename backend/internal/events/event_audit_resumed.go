// Code generated by tools/genevents. DO NOT EDIT.

package events

import (
	"errors"
	"fmt"
	"time"
)

// AuditResumed is emitted when a audit operation transitions to resumed.
type AuditResumed struct {
	TenantID     string
	SessionID    string
	ResourceID   string
	Region       string
	OccurredAt   time.Time
	DurationMs   int64
	AttemptCount int
	BytesIn      int64
	BytesOut     int64
	StatusCode   int
	Message      string
	Labels       map[string]string
}

func (e *AuditResumed) EventName() string { return "audit.resumed" }

func (e *AuditResumed) Validate() error {
	if e.TenantID == "" {
		return errors.New("audit.resumed: tenant id is required")
	}
	if e.OccurredAt.IsZero() {
		return errors.New("audit.resumed: occurred-at timestamp is required")
	}
	if e.OccurredAt.After(time.Now().Add(5 * time.Minute)) {
		return fmt.Errorf("audit.resumed: occurred-at %s is in the future", e.OccurredAt)
	}
	if e.DurationMs < 0 {
		return fmt.Errorf("audit.resumed: negative duration %d", e.DurationMs)
	}
	if e.AttemptCount < 0 {
		return fmt.Errorf("audit.resumed: negative attempt count %d", e.AttemptCount)
	}
	if e.BytesIn < 0 || e.BytesOut < 0 {
		return errors.New("audit.resumed: negative byte counts")
	}
	if e.StatusCode != 0 && (e.StatusCode < 100 || e.StatusCode > 599) {
		return fmt.Errorf("audit.resumed: implausible status code %d", e.StatusCode)
	}
	for k, v := range e.Labels {
		if k == "" {
			return errors.New("audit.resumed: empty label key")
		}
		if len(v) > 256 {
			return fmt.Errorf("audit.resumed: label %q exceeds 256 bytes", k)
		}
	}
	return nil
}

func (e *AuditResumed) Fields() map[string]any {
	fields := map[string]any{
		"event":         e.EventName(),
		"tenant_id":     e.TenantID,
		"session_id":    e.SessionID,
		"resource_id":   e.ResourceID,
		"region":        e.Region,
		"occurred_at":   e.OccurredAt,
		"duration_ms":   e.DurationMs,
		"attempt_count": e.AttemptCount,
		"bytes_in":      e.BytesIn,
		"bytes_out":     e.BytesOut,
		"status_code":   e.StatusCode,
		"message":       e.Message,
	}
	for k, v := range e.Labels {
		fields["label."+k] = v
	}
	return fields
}

func init() {
	register("audit.resumed", func() Event { return &AuditResumed{} })
}
